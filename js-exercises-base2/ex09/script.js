
document.getElementById('push').addEventListener('click', () => {
    function testWhile(){
        saisie=prompt('Entrer une chaîne de caractères contenant la lettre p');
        alert('"' + saisie + '"');
        while (saisie =);
        {
        saisie=prompt('Entrer une chaîne de caractères contenant la lettre p');
        }
    }
    testWhile();
});


// Exercice 9 - utilisation de la boucle while

// Créer un bouton. Au click de ce bouton, lancer la fonction testWhile. Cette fonction demande à
// l'utilisateur de saisir une chaîne de caractères contenant la lettre p, et le redemande tant que
// l'utilisateur ne l'a pas fait. Il affiche ensuite la chaîne saisie entre guillemets
